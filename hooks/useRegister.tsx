const useRegister = () => {
  const register = async () => {
    try {
      // Call api
    } catch (error) {
      console.error(error);
    }
  };
  return {
    register,
  };
};

export default useRegister;
